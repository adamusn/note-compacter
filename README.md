# Note Compacter — Stage 1 (Windows)

This repository contains the Stage 1 environment and storage baseline.
Run "npm install" once, then "npm run dev". An Electron window will open
and display your userData storage path and the Stage 1 marker file.
